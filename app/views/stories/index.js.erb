$('#stories').append('<%= escape_javascript(render "stories/stories" )%>');
$('#pagination a').html('<%= escape_javascript(link_to_next_page(@stories, t("helpers.links.load_more"), :remote => true).to_s) %>');
if ($('#pagination a').html() == ''){
  $('#pagination a').hide();
}else{
  $('#pagination a').show();
}